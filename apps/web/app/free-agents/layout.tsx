import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "2026 NBA Free Agents — Signable Under Real CBA Rules | Over the Apron",
  description:
    "Every current NBA free agent with Bird rights, cap holds, and the exact mechanisms each team can legally use to sign them.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
