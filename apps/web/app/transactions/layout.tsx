import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "2026 NBA Offseason Transactions — Every Real Move | Over the Apron",
  description:
    "The full 2026 NBA offseason transaction log — trades, signings, sign-and-trades — as the engine models them.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
